.messages
  each type in Object.keys(messages)
    each message in messages[type]
      div(class="alert alert-"+type+" fade show") #{message}
        button.close(type='button', data-dismiss='alert')
          i.far.fa-times-circle
